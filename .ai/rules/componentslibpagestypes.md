---
paths:
  - 'app/{Enums,Http/Controllers,Http/Requests,Mcp,Models}/**,resources/js/{components,lib,pages,types}/**'
---

# Componentslibpagestypes

## Source editors keep source and rendered HTML
Plain text and Markdown are team-scoped EmailEditor modes. Persist editable author input in source, persist generated email-safe markup in html for delivery/tracking, and keep design exclusive to EmailBuilder.js. Plain text campaigns also mirror source into plain_text.
